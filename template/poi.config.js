// optional config file for poi
/*const webpack = require.main.require('webpack')

module.exports = {
  webpack:function(config) {
    config.plugins.push(new webpack.IgnorePlugin(/^sp-request$/))
  }
}
*/
